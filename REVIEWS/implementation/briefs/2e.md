## Tickets: robustness follow-ups from wave 1

1. **F-H25:** `Supervisor::new`, or its caller, registers its state root the way `resolve_state_root` and `serve_agent_with` now do (lane B2). Also, the local watcher (`watch_tree` in `src/endpoint/local.rs`) skips the state root when it sits inside a root, matching the scanner's backstop.
2. **F-H5:** the `src/persist.rs` writer thread uses `crate::threads::spawn_deep`.
3. **F-M-STAGE M-30, the part lane A2 left:** a background sweep of the whole root for stale `.autobahn-tmp-apply-*` leftovers, after the first scan. Use the age and process-id rule in the ticket.
4. **LOCAL-01 wiring:** make sure `fsutil::sweep_private_tmp(state_root, TMP_MAX_AGE)` runs once at supervisor and one-shot startup, if no lane wired it (grep first).
5. **LOCAL-03 leftovers:** check whether lane A1's rebased work did the received-temporary and wire-error parts of `LOCAL-03-staging-temporaries.md`; do whatever it didn't. Then unify `copy_verifying` and `copy_into_private` in `src/endpoint/local.rs` if they now do the same job.
6. **T2-1:** where a call site knows the base length, use `Signature::validate_for_base(len)`, from lane E2. Report in your notes where it can't be used.

## Region you own

- `src/endpoint/local.rs`: the watcher's state-root skip, the startup sweep, the copy helpers, and the temporaries
- `src/persist.rs`
- `Supervisor::new` and startup in `src/supervisor/mod.rs`, *not* endpoint opening, which is lane 2a's
- `src/scan/`: the sweep hook only

Lane 2a adds progress-counter increments to scan and supply loops. Don't edit those loops' bodies.
