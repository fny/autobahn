# F4 notes
- The bench harness (bench/harness) was not rustfmt-clean at wave-0 (agents.rs, partitions.rs, sampler.rs, walk.rs). I format only what I write; I did not reformat the untouched code, to keep diffs small. Clippy -D warnings is clean there.
- smoke.sh hard-codes harness/target/release/benchmark, so run it with `env -u CARGO_TARGET_DIR`.
- BENCH-1: `floor` no longer takes `--dest-root`; it sends relative probe paths and the observer's required `--root` decides where they land. Callers (smoke.sh, job.py, ab.sh, orchestrate.py, verify/soak.sh, verify/fanout.sh) updated. Verify requests (agents) still carry absolute paths: they only read, so they were left alone.
- BENCH-2: `--corpus DIR` is a read-only pristine source; working copy is $WORK/corpus; `scrub` guards every rm -rf.
- BENCH-3: orchestrate.run() takes argv lists only; new helpers ssh()/scp()/authorize_source_key()/checked_*; launch.py uses them. Test: `python3 bench/test_orchestrate.py`. Not exercised against AWS (no credentials) — the integrator/operator should do one bake/run smoke before trusting it.
- BENCH-4: took the "limit the flag" option: --remote must see this machine's filesystem (token read-back over ssh). The --remote localhost check ran through an ssh shim (no ssh to localhost here). Cold-sync 600s timeout path not exercised (would take 10 minutes); the early-exit path was.
- BENCH-5: bench-1789947877 job_start records lack `destinations`; the six contaminated jobs are inferred from plan.json group widths (exactly six match). Those six now also leave that run's latency tables (they were never flagged before) — headline numbers for that run shift. Edited examples/cycle_cost.rs (comment, output line, extra column; no lane owns it). Pre-existing: cycle_cost panics intermittently on tiny trees ("one edit should yield one transition" / line 83) before and after my change — not investigated, out of region.
- BENCH-6: also fixed `conflicts`→`issues` and `resolve --yes` in scripts/mi, and old mode names in the verify configs. Local scripts no longer `pkill -x autobahn` machine-wide (killed other lanes' processes on a shared host); they signal their own setsid process group. EC2-only scripts + differential.sh verified statically only (shellcheck, bash -n, broken-binary preflight).
- bench/verify/fanout.sh and bench/alpha-bench.sh still have shellcheck findings; not in any of my tickets, left alone.
