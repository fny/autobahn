## Tickets, in order

1. `HYG-1-stale-artifacts.md`: `git rm dist/agents/autobahn-linux-x86_64`, add `/dist/` to `.gitignore`, derive the plist version in `apps/macos/build.sh`, and put a placeholder in `Info.plist`. The release-workflow check belongs to lane F3.
2. `CI-05-keychain-order.md`: **the script half**.
   - `apps/macos/build.sh --unsigned` builds the bundle without signing.
   - `apps/macos/release.sh --sign-only <app>` signs, notarises and staples an existing bundle.
   - `release.sh` with no arguments still builds and signs in one go.

   Lane F3 wires those exact flags into the workflow.

## Region you own

- `apps/macos/`
- `dist/`
- `.gitignore`

This is Linux, so macOS tools like `codesign`, `plutil` and `xcrun` aren't available. Check your scripts with `shellcheck` and `bash -n`. Make the version-derivation logic testable on Linux, for example by factoring it into a function that prints the version, and test that. Note in your done file what only a Mac can verify.
