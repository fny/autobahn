# F5 notes
- HYG-1: version is derived by sed from a template (placeholders __AUTOBAHN_VERSION__ / __AUTOBAHN_ICON__) rather than plutil/PlistBuddy, so build.sh runs end-to-end on Linux under apps/macos/test.sh with stubbed cargo/xcrun/codesign. PlistBuddy is gone from build.sh; CFBundleIconName now lives in the template.
- apps/macos/test.sh is a plain shell test, not wired into cargo test or CI (CI is F3's). Suggest F3/integrator add `apps/macos/test.sh` as a step (runs on Linux too).
- The version check in release.yml (plist vs tag) is F3's.
- CI-05: build.sh [--unsigned] [app]; release.sh [--sign-only <app>]. Paths now resolve against the caller's cwd (build.sh used to take them relative to the repo root). release.sh (no args) = check creds, build.sh --unsigned, then the same strict sign path as --sign-only (timestamp required, no fallback; lsregister no longer runs in that mode).
- docs/macos-app.md and docs/releases.md don't mention the new flags (docs are outside F5's region).
