# Lane 2e notes

- F-H25: Supervisor::new calls scan::exclude_state_root; watch_tree skips state-root identities (start and children). Made scan::state_root_identities pub(crate) (one-word edit in src/scan/mod.rs).
- LOCAL-01 wiring: already done by wave 1 — paths::prepare_state_root sweeps ~/.autobahn/tmp and is called by resolve_state_root, run_sync, run_sync_config and the supervisor startup in main.rs. Nothing to do.
- LOCAL-03: lane A1's rebased commit 408c16a did the received-temporary, random-name and wire-error parts, and copy_verifying is already removed.
- F-H5: persist writer uses spawn_deep. Test uses 100k levels (encoding a 20k chain fits in 2 MiB; 50k overflows).
- M-30 startup sweep: LocalEndpoint::sweep_leftovers_once, called after scan/scan_verified; walks the snapshot's directories on a background thread, once per root per process (static set). Reuses A2's stale_publish_leftover rule. No change in src/scan.
- LOCAL-03 leftover: unified stage_locally's loop and copy_into_private into copy_verifying (new, with a CopyFailure {Read, Write} enum). Refactor only.
- T2-1 (validate_for_base): skipped, no call site where it adds anything.
  * endpoint/local.rs begin_supply: the only signature that comes from a peer (StagingNeed.signature, in SupplyOpen). The supplier doesn't know the destination base's length, so it can only call validate(), which rsync::deltify already does. Bounding it would need a base length in StagingNeed, which is a wire change for lane 2a's epoch bump if wanted.
  * endpoint/local.rs push_frames/open_receive_file: the signature is this side's own, from stage_begin. Checking it against the reopened base's length only catches a shrunk base early, which M-31 (e2342d4) already handles per file when a patch fails, and it would refuse some patches that would have worked.
  * endpoint/remote.rs reassemble and transport/mod.rs scan-delta production: signatures computed locally from the base itself (E2 already range-checks header.block_size), so the check would always pass.
