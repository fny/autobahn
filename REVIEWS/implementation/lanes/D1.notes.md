# Lane D1 notes

## F-H1 stage 1
- Guard implemented by *simulating the next cycle*: after scanning every side, resolve builds each
  session's post-retirement trees, runs `tree::reconcile` against the session's ancestor
  (`session::ancestor::peek`, read-only; unreadable ancestor => bail, nothing touched) in the
  session's mode, applies the transitions, and refuses any path whose kept version would not
  survive. This covers the ticket's "winner equals ancestor" rule, the one-way and
  two-way-alpha-strict refusals, and `--keep both`, and it stays correct for two-way-paranoid,
  where the untouched-large-directory restore rule means an unchanged winner *does* survive
  (the ticket's plain "winner == ancestor" rule would have refused the emptied-directory flow
  that the reconcile.rs comment documents).
- Tests added to tests/supervisor.rs beside the existing resolve tests.

## F-H30
- New lib module src/invocation.rs (resolve_command, diff_command, with_options); lib.rs mod line.
- Group placed after `--` too, so no config refusal of '-' group names was added (config.rs is D2's).
- Tray (feature `tray`) can't compile here (no glib); call sites type-checked in a scratch crate. Integrator: build with --features tray on macOS.


## F-H29
- `home_quoted`: a `~/` root keeps `"$HOME"/` outside the quotes (a quoted `~` would not expand). Beyond the ticket.

## F-M-OUT (M-6 only)
- display_safe applied at render in status/issues/resolve output, shop rows/ticker/footer/notices, tray labels/notifications.
- Backstop: `pager::only_colour` passes only SGR sequences; used by pager paint and shop grid.
- `pasteable()` for the `resolve` suggestion line in issues (quotes, `./` for leading '-', '=' quoted for zsh).
- Not done here: print_report (sync, D3), logs/stderr/prune (E2).

## HYG-2
- src/style.rs (bin module): stdout_level()/screen_level()/apply()/emit(). `colour_enabled()` named in ticket became stdout_level().
- run_doctor colour and run_watch isatty are D3's region, not routed yet.
- No --color flag.

## Review follow-up (1250595)
- Fixed false refusal in fan-out when alpha has no copy; nested paths deduped; blocked paths not retired on other sides.
