## Tickets, in order

1. `F-H2-manual-sync-topology.md`: extract `check_session_topology` in `src/config.rs`, make containment component-aware (M-39), and call it from `run_sync`. The call site in `src/main.rs` is allowed.
2. `F-H25-root-contains-state.md`: **only the planning-time refusal**, in the same topology function. Call it where the state root is known: `Supervisor::new` or its callers, `run_sync_config`, `run_sync`, and `check_startable`. Keep those call sites to a line or two each. Lane B2 owns the scanner backstop.
3. `F-H12-session-identity.md`:
   - the cross-session overlap check compares plan indices or identifiers, not `display()`;
   - `writable` becomes an exhaustive `match` over every mode;
   - `SessionPlan::display()` adds the path when two plans in a group share a host.

   The progress key fix belongs to lane D3. A full `SessionKey` threaded through control and alerts is out of scope for now: write it in your notes as follow-up.
4. `OPS-6-secrets-warning.md`

## Region you own

- `src/config.rs`, except the `ON_ALERT_EXAMPLE` string, which lane E3 edits, and `Config::load`'s writability warning, which lane F2 adds.
- The small call sites listed above.
