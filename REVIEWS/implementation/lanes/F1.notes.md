# F1 notes

- Started from a9caba5 (wave-0).
- REL-1 step 1 done. INTEGRATOR/F3: release.yml must add `scripts/install.sh` to the `gh release create` asset list (as `install.sh`), else the README one-liner (now releases/latest/download/install.sh) 404s. I edited docs/releases.md step 5 to say it is published.
- install.sh gained AUTOBAHN_RELEASE_BASE (base URL override; skips gh) for the local fake-release test in tests/install_script.rs (new file).
- README line ~201 used the `release` branch URL; changed to the release asset too.
- LOCAL-06 done: Verified handle + Incoming install temporary in update.rs; checksum tool reads the handle via stdin (no new dependency).
- F-M-UPD done. update::install() with Fetch/Service seams; `update --retarget` flag added in main.rs; start/restart now check_startable(service::installed_config()) in main.rs.
  - After a confirmed update autobahn.previous is now deleted (ticket's decision); before, it was always kept.
  - Pre-existing, not fixed: update refreshes agents into the updater's default_state_root, not the service's AUTOBAHN_HOME, if those differ.
  - installed_config() uses only an absolute --config; AUTOBAHN_HOME-only differences between shell and unit are not considered (peer-mode check preserved).
  - macOS platform module could not be compiled here (no apple target); reviewed by eye. Integrator: check the mac CI build.
- LOCAL-08 done (fcef177). New src/root.rs. Small edits outside region: main.rs (global --allow-root on Cli + refuse_root() before dispatch), transport/mod.rs serve_channel (check_agent before create_endpoint — E2/0b region, 2 lines), config.rs Advanced.allow_root (D2 region, 3 lines), lib.rs mod line, docs/configuration.md + RETAINED.md one sentence each.
  - Verified as real root via sudo (lib root:: tests and tests/root.rs run under sudo pass).
- Full suite: all pass except the known flake a_standing_watch_lets_the_next_cycle_skip_the_beta_scan (passes alone).
