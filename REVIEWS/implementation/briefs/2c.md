## Tickets

`F-M-TEST-test-suite.md`: the M-46, M-47 and M-54 sections. Lane 0b already did M-53.

- **M-46:** the standing-watch test loops until alpha holds the edit, bounded. It still asserts that a scan of beta happened. Then run it 200 times in a row (`isolated lane-2c cargo test --release --test e2e a_standing_watch -- --test-threads=1`, in a loop) and report the pass count.
- **M-47:** tighten the cut oracle as the ticket says, and add the mutation check: an injected rollback of `modify.txt` must fail it. Extend the fault matrix to a deletion and a file-to-directory change, as far as it reasonably goes.
- **M-54:** `#[ignore = "…"]` on the TLC replay tests and the APFS-dependent test. The CI spec job already runs them with `--include-ignored`; check `.github/workflows/ci.yml`.

Skip the arm64 part. The integrator runs it separately.

## Region you own

- `tests/e2e.rs`
- `tests/spec_replay.rs`
- `tests/spec_peering_replay.rs`
- any test-only helpers in `tests/common/`
