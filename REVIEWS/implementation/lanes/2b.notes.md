# Lane 2b notes

## HYG-3 (78f2dc9)
- Lints: `empty_line_after_doc_comments` + `doc_lazy_continuation` warned explicitly in lib.rs/main.rs; both were already clean (the misplaced docs were fused, not blank-separated, so no lint catches them). `too_long_first_doc_paragraph` tried: 60 style hits, left off.
- `sanitize` claim checked: true since ba303f4 (every transition goes through `synchronized()`); left unchanged.
- One user-visible string changed: run_availability's restart advice ("a running supervisor applies it; with `reload = false`, `autobahn restart`"), plus `restart`'s clap help. D1/D3 own main.rs: comment/text-only edits in their functions (run_clean/run_init/run_resolve/node_at/peer_plans/load_config/pasteable/run_sync comment).
- `node_at` in main.rs still carries a stray `#[allow(clippy::too_many_arguments)]` (2 params) above its doc; attribute left alone (not text).
- Doc-only edits outside the lane's files: config.rs, service.rs, supervisor/{mod,control}.rs, tree/mod.rs, endpoint/mod.rs (staging path is `<session>-<side>`).

## OPS-5 (147d37a)
- No symlink section existed in configuration.md; added `### Symbolic links` under Session settings. Hook env paragraph in alerts.md describes launchd's fixed PATH (from service.rs render_plist) and systemd's user-manager env.

## T1 docs (c52c0bc)
- New invariant I11 (confinement, genuine or not) rather than renumbering; safety.md section placed after I10. Threat-model note split into two tiers. I9 notes `Signature::validate_for_base` has no production caller (E2's note: receiver doesn't know base length).

## Follow-ups (f049953)
- `apps/macos/build.sh` still runs `cargo build` without `--locked` (F5's file, CI-07 leftover) — not a doc; integrator/F5 should add it.
- README one-liner already on releases/latest/download/install.sh (F1).
- allow_root was already in configuration.md's table; added a "What is refused" section and safety.md bullets.
- Review pass found 5 doc overstatements; fixed in 469bebd.
