# B1 notes

- Started 2026-09-24 on lane-B1 (base a9caba5).
- F-H3 committed. Seen-generation rule is stricter than the ticket (own announcements must be exactly lease+1, lease+2), see commit. observer tests' harness now watches (want_watching + is_watching).
- F-M-OBS committed (3125675), one commit for M-23..M-28 and L-27.
  - M-28 departure: no putting marks back. A failed or refused incremental walk forces a full next scan, which reads everything the marks named.
  - M-26: "appears in status" is covered only by `is_watching()` = false (sent as `watching` in the AwaitChanges reply) plus a single log line. No status display shows watch state today, and adding one is outside B1.
  - M-25: added `IgnoreSet::ignored_within` / `IgnoreSet::traversal` in src/scan/ignore.rs; src/scan/mod.rs `entry_ignored` + `probe_entry` now call them (~10 lines in B2's file, possible textual conflict with LOCAL-09 in probe_entry).
  - A missing root is retried at the next ensure_watching, not after 30s, so a replaced root is re-watched promptly.
- Flake a_standing_watch_lets_the_next_cycle_skip_the_beta_scan: measured 3/8 failures on the wave-0 observer/local vs 2/6 with my changes, so no regression seen.
