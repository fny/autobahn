# C1 notes

- Started from a9caba5 (wave-0). Tickets: F-C1, F-H8 (reconcile part), M-33, L-31.
- M-33 premise confirmed by reading: local `remove_directory` matches each on-disk entry against `old`; an `Untracked` child in `old` reaches `remove_entry` → "refusing to remove unsynchronizable content", so the directory survives and the deletion repeats every cycle.
- L-31 premise confirmed by reading: `one-way-conflict` (OneWaySafe) keeps beta's edit on beta and forgets the ancestor; it never writes alpha. The doc row is wrong.
- F-C1 committed. The proptest generator now derives alpha/beta from the ancestor by mutation (Untracked at every depth, root included). All properties run in all six modes (TwoWayStrict was missing before). New property `no_change_is_lost_silently`, with `silent_losses` spelling out the exemptions (alpha-wins modes may overwrite beta's synchronizable content; an ignored entry not in the ancestor may go with its deleted directory; content under an untracked entry on the other side is shielded; beta's changes in one-way modes don't propagate). Checked at 20000 cases; committed at 1024 (the whole module still runs in under a second).
- INVARIANTS.md I7 edited (statement, checked-by, boundary) as F-C1 asks. That file is outside my code region, so the integrator may see small conflicts with other lanes there.
- F-H8 reconcile: `blocking(path, ancestor, incoming, unsync)`. The comment assumes A2's endpoint change (only pattern-ignored entries go with a deleted directory). If A2's part doesn't land, the comment overstates what the endpoint does.
- M-33: `synchronized(side)` helper in reconcile.rs. Both one-way branches now use `old: beta_sync`. New property `a_transition_expects_what_its_side_synchronizes`. The e2e test `a_one_way_deletion_goes_through_ignored_content_on_beta` was added to tests/e2e.rs (a new function only); it failed in one-way-alpha before the fix.
- L-31: the code was right and the doc was wrong. `one-way-conflict` keeps beta's edit on beta, reports nothing and clears the ancestor entry. The modes.md cell and the one-way-conflict paragraph are corrected, and the test is `alpha_deleting_a_file_beta_edited_matches_the_modes_table`.
- Full suite green at 58e30fd.
