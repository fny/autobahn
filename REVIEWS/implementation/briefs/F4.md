## Tickets, in order

`BENCH-1` to `BENCH-6` (files `REVIEWS/fixes/BENCH-*.md`), then `LOCAL-07-bench-work-dirs.md`.

## Region you own

- everything under `bench/`
- `scripts/mi`

The benchmark harness is its own Cargo package, in `bench/harness`. Build and test it there. Don't run anything against AWS; there are no credentials here, and none are needed. Verify the scripts with `shellcheck`, `bash -n` and `python3 -m py_compile`, plus the local tests each ticket asks for.
