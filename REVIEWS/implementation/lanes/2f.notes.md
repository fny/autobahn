# lane 2f notes
- T1: added apps/macos/test.sh as step in ci.yml linux job (after Formatting).
- T2: --locked in apps/macos/build.sh; test.sh asserts it (failed before fix).
- T3: cargo fmt on bench/harness (formatting-only commit).
- T4: shellcheck-clean fanout.sh, alpha-bench.sh. Note: fanout.sh still hard-codes BM=/home/ubuntu/Workspace/... (not in ticket scope; left alone).
- T5: cycle_cost flake = harness timing, not engine bug. Observer (src/endpoint/observer.rs scan_inner) serves the published snapshot until watcher generation advances; inotify delivery is async, so an immediate rescan saw no edit -> 0 transitions (reproduced 25/40 on a 9-entry tree). Fix in example: await_change_since before the timed rescan, retry (<=10) if rescan still shows old digest. 0/60 after. No engine change.
- T6: isolation check already exists in ci.yml linux job ("Snapshot ~/.autobahn" / "The tests left ~/.autobahn unchanged"); no change needed.
