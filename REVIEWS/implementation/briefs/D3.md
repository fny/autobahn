## Tickets, in order

1. `OPS-1-sync-exit-codes.md`
2. `OPS-2-clean-keeps-disabled.md`
3. From `F-L-MISC-small-correctness.md`:
   - L-7, `~` expansion in `run_sync`;
   - L-28, the warning for a wildcard re-include under an ignored directory, in `src/scan/ignore.rs` plus a line in `docs/ignores.md`;
   - L-29, `select` keeping one relative path per plan.
4. From `F-H12-session-identity.md`, **only the L-30 part**: progress keyed per session, not by `(group, host)`. It's around `src/main.rs:3249`, in the progress display.

## Region you own

In `src/main.rs`:
- `run_sync` (non-topology), `run_sync_config`'s exit code, and the exit-code plumbing;
- `run_clean`;
- `select` and the selection helpers;
- the progress display.

Also `src/scan/ignore.rs` and `docs/ignores.md`. Lanes D1, D2, E1 and F2 edit other functions in `src/main.rs`, so keep your diff inside these functions.
