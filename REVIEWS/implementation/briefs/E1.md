## Tickets, in order

1. `F-H13-empty-live-config.md` and `OPS-7-reload-by-session.md` together, including the L-25 pool eviction that OPS-7 describes and the L-23 fix from `F-L-MISC-small-correctness.md`: reload validates the bytes it compared.
2. `F-M-SUP-supervisor-robustness.md`:
   - **M-34:** contain panics per session with `catch_unwind` around the worker's cycle loop body, restart with backoff, and halt after repeated panics. Make logging unable to panic.
   - **M-38:** a control request returning the running supervisor's session inventory and any rejection notice. `run_status` in `src/main.rs`, and the shop and tray data loading, use it when a supervisor answers.

   M-12 belongs to lane E2, and M-35 to lane E3.
3. `F-M-OUT-untrusted-text.md`: **only the log part (M-7)**. Apply `crate::text::display_safe` to interpolated values in `src/logging.rs`.

## Region you own

- `src/supervisor/mod.rs`, except the session-worker *spawn call*, whose stack size lane B2 changes: put `catch_unwind` inside the closure body.
- `src/supervisor/reload.rs`
- the server side of `src/supervisor/control.rs`
- `src/logging.rs`
- `run_status`'s data source in `src/main.rs`
- the shop and tray refresh and data-loading functions

The pool's slot removal lives in `src/transport/mux.rs`. Add only a small method there, since lane E2 owns that file.
