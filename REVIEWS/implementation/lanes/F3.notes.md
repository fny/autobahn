# F3 notes

- tla2tools pinned to v1.7.4 (the current `releases/latest`, byte-identical: sha256 936a2620…0e88). v1.8.0 is a prerelease.
- Actions pinned within their current majors (checkout v4.4.0, setup-java v4.9.1, upload-artifact v4.6.2, download-artifact v4.3.0, rust-cache v2.9.2, rust-toolchain `stable` branch). Major bumps are left to Dependabot (CI-10), since no workflow can be run here.
- CI-03: spec/README.md:16 still reads true but doesn't mention the pin or TLA2TOOLS_TRUST; it's outside my region, so it's left for the docs lane (2b).
- CI-07 (partial): `--locked` added to every cargo build/test/clippy in the workflows, and to scripts/build-agents.sh (unowned, a one-word edit, named in the commit). NOT done, since they're outside my region: `apps/macos/build.sh:23` (F5 owns it; the CI mac job and the release app build go through it) and the build commands in docs/development.md and README.md (docs lane 2b). The integrator should make sure F5 or 2b adds them.
- CI-08: the gate uses the Actions API (`actions: read`) instead of check runs (`checks: read`), because release.yml's own jobs are also named linux/mac, and a re-pushed tag leaves their check runs on the same commit. Tested locally with a stub gh over fixture JSON (green, mac skipped, no run, split across runs, one failed, API error). docs/releases.md should mention the gate (docs lane).
- REL-1 step 1 (workflow part only): install.sh is published as a release asset and listed in SHA256SUMS. The README one-liner pointing at releases/latest/download/install.sh belongs to F1 or the docs lane.
- HYG-1: the plist-vs-tag check is in the mac release job, not the version job the ticket names, because the built app only exists there. It runs before signing.
