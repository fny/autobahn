## Tickets, in order

1. `F-H3-stale-baseline-offer.md`
2. `F-M-OBS-observer-and-watcher.md`, all of it: M-23 to M-28 and L-27.

## Region you own

- `src/endpoint/observer.rs`.
- In `src/endpoint/local.rs`:
  - the watcher code: `watch_tree`, the watch extension around the event handling, `PendingChanges`, and watcher setup and health;
  - `seen_generation` handling in `scan`, `scan_verified` and `transition`: the lease-generation capture, the `invalidate` return value, and the `offer_baseline` call.

  Don't touch the rest of `transition`.

For M-25, share the scanner's `holds_a_re_inclusion` decision. Calling it from `src/scan/` is fine. Moving it, if needed, is a small allowed edit.
