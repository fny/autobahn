## Tickets, in order

1. `LOCAL-02-state-and-config-permissions.md`
2. `LOCAL-04-diff-scratch.md`
3. `LOCAL-05-control-socket-fallback.md`

## Region you own

- `src/persist.rs`
- `src/paths.rs`
- `Config::load`'s writability warning in `src/config.rs`: a small addition
- `run_diff` in `src/main.rs`
- the tray's "Show diff" file writing in `src/tray.rs`: one function
- the fallback-path and bind functions in `src/supervisor/control.rs`
- the state-root preparation at startup: a small function called from the entry points

Use `crate::fsutil`, which lane 0c added.
