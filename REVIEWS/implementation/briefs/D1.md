## Tickets, in order

1. `F-H1-resolve-keeps-the-winner.md`: **stage 1 only**, the guard. Stage 2 is not in scope.
2. `F-H29-fix-command-quoting.md`. Use `crate::text::shell_quote`, which lane 0d added.
3. `F-H30-resolve-argument-separator.md`
4. `F-M-OUT-untrusted-text.md`: **only the terminal part (M-6)**. Apply `crate::text::display_safe` to tree- and peer-derived strings in `status` and `issues` printing (`src/main.rs`), `src/shop.rs`, `src/pager.rs`, and the tray's displayed labels. Logs and relayed stderr belong to lane E2, and prune to E2.
5. `HYG-2-no-color.md`

## Region you own

In `src/main.rs`:
- `run_resolve` and `node_at`;
- `blocked_fix`;
- the status and issues printing functions, except `run_status`'s *data source*, which lane E1 changes for M-38. Keep your edits in `run_status` to display formatting.

Also:
- `src/shop.rs`, except its refresh and data-loading, which lane E1 may touch;
- `src/pager.rs`;
- `src/tray.rs` command building and labels;
- a new `src/style.rs` for HYG-2.
