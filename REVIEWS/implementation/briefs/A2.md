## Tickets, in order

1. `T1-3-confined-read-and-rename.md`, including the L-20 fix from `F-L-MISC-small-correctness.md` (keep-both `rename` uses `publish_rename(…, false)`).
2. `T1-4-reserved-names-and-staging-root.md`, together with `LOCAL-03-staging-temporaries.md` **except** the `open_receive_file` and `stage_locally` parts, which lane A1 owns. You own `temporary_name` and the copy-publish temporaries.
3. `F-H8-excluded-content-deletion.md`: **only** the endpoint part, the excluded-content branch of `remove_directory`. The reconcile part belongs to lane C1.
4. `F-M-STAGE-staging-and-apply.md`: M-29 (publish race) and M-30 (leftover `.autobahn-tmp-apply-*`). Not M-31, which is lane A1's.
5. `LOCAL-11-hardlink-chmod.md`

## Region you own

In `src/endpoint/local.rs`:
- `read_file`, `rename`, a new `resolve_confined`, and the removal of `resolve_relative`;
- `validate_name` and `validate_path`;
- `staging_root_for`, `temporary_name`, a new `prepare_staging_root`, and the `create_dir_all` at the top of `stage_begin` (replace just that call);
- `remove_directory` and `remove_entry`;
- the publish path: `publish_file`, `publish_rename`, the staged-use counter, and the copy-publish;
- the in-place chmod.

T1-4 needs `crate::scan::autobahn_temporary` to be `pub(crate)`. That one-line visibility change in `src/scan/mod.rs` is allowed. Lane 0c's `crate::fsutil` helpers exist: use them.
