# D3 notes

## OPS-1
- Exit code 2 carried as a typed error `Unsettled` in main.rs, downcast in `main()`; keeps every other command's `Result<()>` untouched.
- Manual one-shot `sync a b` counts the final cycle's conflicts + all four problem lists; `--watch` never exits normally so unaffected.
- `resolve` exit scheme ("consider") not done: resolve is outside D3's region.
- docs/commands.md edited (exit-code table under "One-off syncs and scripting").

## OPS-2
- Keep-set = active plans ∪ plans of every disabled session. Disabled plans are derived by re-loading the config once per group and planning that group alone with `disabled_hosts` cleared (Config isn't Clone and config.rs isn't mine). One broken group costs only itself.
- Ticket premise "as clean already asks for other deletions" is wrong: clean never asked. Added a y/N prompt for `--include-disabled` (refuses without a tty) and `--yes` to skip; dry-run never asks.
- Broken disabled group: while one is present (and no --include-disabled) every otherwise-removable session/status/endpoint lock is kept with "kept …: could not tell what it belongs to". Staging is left alone in that case only if it's not "known stale" (retired set empty then), age-based removal still applies.
- Note for integrator: `clean` removes unheld endpoint locks in the default state root, which the test binary shares across tests; harmless (held locks are skipped).
- docs/state.md updated.

## L-7
- `run_sync`'s local specs go through `paths::expand_tilde` before `resolve_for_identity`, so identity, lock and root all see the expanded path.

## L-28
- New `IgnoreSet::ineffective_negations()` in src/scan/ignore.rs; decides each literal leading directory exactly as the scanner does (region inversion + holds_a_re_inclusion), so a dir opened by a wildcard-free negation is not reported.
- **Out-of-region call site (D2's config.rs, `Config::plans`)**: the `Ok(compiled)` arm of the ignore compile now `eprintln!`s each warning ("autobahn: warning: group 'g': …"). config.rs has no warning channel; plans() runs on every command/reload, so the warning repeats per load (and `clean` prints it once more per group because of OPS-2's per-group planning). If D2 adds a warnings channel, move it there.
- Second behaviour (`!*.md` inside a walked region) checked against real `git status`: matches git's result for the `vendor/*` spelling; documented, not changed. Fixture test in ignore.rs.

## L-29
- `Selection` now carries `relatives` (one per plan) plus `relative` = the remainder all plans agree on (None when nested groups disagree). New `relative_in(selection, index, explicit)`.
- `run_diff` (unowned; call-site edit) reads the path per plan.
- `relative_path` (used by `run_resolve`) refuses a disagreeing path selector with a clear message; resolve already refuses multi-group selections before that, so no change in practice. run_resolve untouched (D1).
- **Integrator / D1:** `run_issues` still uses `selection.relative`; for a path in nested groups it now scopes to the whole groups (superset) instead of the wrong subpath. A per-plan scope (`selection.relatives[i]`) there belongs to D1's issues region.

## L-30 (from F-H12)
- Progress answer now carries the session identifier: `SessionProgress.session` and `control::Entry.session` (src/supervisor/control.rs, E1), populated with `plan.identifier()` where the registry is built (src/supervisor/mod.rs, E1). New `control::progress_of(sessions, identifier)`.
- Lookups switched to it: `render_status` (both, main.rs, mine) and `status_report` (supervisor/mod.rs, E1 — one-line change; the JSON status/tray/shop had the same (group, host) bug).
- bincode wire change of `ControlResponse::Progress`: a CLI and supervisor of different builds already get "another build is running" via the versioned envelope, so no compat shim.
- **Integrator:** likely textual conflicts with E1 (registry construction in Supervisor, `Entry` struct) and D2 (F-H12's SessionKey work may supersede this; if D2 introduces a SessionKey, `session` should become it).
