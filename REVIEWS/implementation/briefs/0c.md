## Tickets

`LOCAL-01-private-fs-helpers.md`.

## Region you own

- A new file, `src/fsutil.rs`.
- One `pub mod fsutil;` line in `src/lib.rs`.
- Nothing else. Other lanes will adopt these helpers later, so don't change any call sites.

## Details

- Implement `private_dir`, `private_file` and `private_tempdir`, the last returning a guard that removes the directory on drop, plus `sweep_private_tmp(state_root, max_age)`.
- Don't add a dependency. For random names, read a few bytes from `/dev/urandom`, which is fine on the Unix platforms autobahn supports.
- Document each function's guarantees in its doc comment.
- The ticket's tests go in the file's test module. Tests that need another uid should detect whether they run as root and skip clearly otherwise.
