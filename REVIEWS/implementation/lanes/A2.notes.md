# A2 notes

## T1-3 (+ L-20)
- `resolve_confined` + free fn `create_confined_parents(root, path, directory_mode)` (not a method; `rename` is in the trait impl).
- read_file cap: MAXIMUM_READ_FILE_SIZE = 256 MiB; larger files are an error, not truncated.
- Keep-both rename now creates parents with the endpoint's directory_mode (0700 default) instead of umask default.
- Reused the existing test hook `between_announce_and_writes` inside `rename` for the L-20 test (no new field).
- Added a symlinked-parent ReadFile check to `an_agent_reads_and_moves_single_entries_within_its_root` in src/transport/mux.rs (E2's file, test only).
- My tests live in a new `mod apply_path_tests` at the end of local.rs to avoid colliding with other lanes' additions to `mod tests`.

## T1-4 + LOCAL-03
- validate_name refuses the whole `.autobahn-tmp` prefix (superset of autobahn_temporary); scanner reports the rest as Problematic so genuine traffic never proposes them.
- scan/mod.rs: `TEMPORARY_PREFIX` and `autobahn_temporary` made pub(crate) (two one-word edits). local.rs's own TEMPORARY_PREFIX const removed; imported from scan.
- prepare_staging_root is a free fn(staging_root, root). Parent created recursively 0700 if missing; verified no-follow only when parent == root (inside-root); other parents may be reached via symlinks (outside the synced tree). Staging dir itself via fsutil::private_dir.
- temporary_name: `.autobahn-tmp-<purpose>-<pid>-<count>-<16 hex>`; token = blake3 keyed hash of counter under per-process random key (no urandom syscall per temp).
- New `copy_into_private(input: &mut File, ...)` used by the copy-publish; old `copy_verifying` left untouched for A1's stage_locally. Integrator may unify them once A1 lands (A1 may switch stage_locally to private_file).
- NOT done (A1's region): open_receive_file/stage_locally private_file, wire error text without staging paths, the "received temporaries are 0600" test.

## F-H8 (endpoint part)
- Added `ignores: IgnoreSet` to LocalEndpoint (set from options in `new`) and `ignores: &'a IgnoreSet` to Transitioner (one line each in `transition()`'s construction and `fork()` — B1 owns transition's generation handling; trivial merge).
- `Transitioner::pattern_ignored(path, is_dir)`: entry or any ancestor directory matched by `IgnoreSet::ignored`.
- Non-pattern untracked entries are left with a `problem` (not disagreement): "left in place, with its directory: excluded by size|by type|by the symlink mode|as a mount point".
- Pre-existing, not fixed: `fs::remove_dir_all` on an ignored directory crosses mount points inside it.
- Shape 2 / reconcile / reconcile.rs comment: C1.

## F-M-STAGE M-29, M-30
- M-29: publish_file opens staged (O_NOFOLLOW) before the use decrement; move path uses fchmod/fstat/hash via handle + (dev,ino) check of the name before rename; copy path copies from the handle. ELOOP on staged → remove + retransfer. New helper `Transitioner::retransfer` and free `content_matches(&mut File, digest)`. `staged_content_matches(path)` kept for stage_begin (A1).
- Test hook: Transitioner field `#[cfg(test)] after_use_counted: Option<&'a (dyn Fn() + Sync)>`; test builds a Transitioner by hand.
- M-30: remove_directory removes `autobahn_temporary` names (files or dirs) as litter. Sweep: `Transitioner::sweep_leftovers(dir)` called from create_change/replace_change (file content only, parents from resolve_parent, i.e. pre-existing dirs), once per dir per transition via shared `swept: &Mutex<HashSet<PathBuf>>` created in transition(). Stale = apply temp, file, mtime >= 15 min, and (pid not running || >= TMP_MAX_AGE).
- Skipped: startup background sweep of the root (would need a full walk or B2's scanner to report skipped temporaries).

## LOCAL-11
- New `Transitioner::change_mode`; nlink read from the O_NOFOLLOW handle after a (dev,ino) check against the validated lstat. nlink>1 → copy_into_private from the handle (digest-checked; mismatch = disagreement) + chmod temp + publish_rename(replace=true) + apply_ownership. EACCES on open with nlink==1 → old path chmod.
