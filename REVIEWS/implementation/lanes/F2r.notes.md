# Lane F2r notes
- 7613321 (LOCAL-02): conflicts in config.rs (integration split Config::load into load + parse, 037635a/0438721) → warning kept in `load` before `parse`; supervisor reload goes through `parse` so a reload doesn't re-warn (startup load still warns). main.rs run_sync: kept integration's topology/OwnState/secrets checks, then F2's private_dir of the session directory after them. resolve_state_root: kept `exclude_state_root`, then `prepare_state_root`.
- 92426da (LOCAL-04): applied cleanly (tray.rs auto-merged beside integration's invocation::diff_command; still uncompiled without the tray feature).
- 6d1913d (LOCAL-05): conflict in control.rs with integration's client timeouts (connect_client, send_within, probe_within, Probe::Unresponsive). Kept all of that; F2's `connect()` became `refuse_another_user(path, &stream)`, applied after connect_client in send_within; probe_within returns Absent for a foreign peer; supervisor_is_running requires the same user on a successful connect (a timeout still counts as running, as integration had it). Root-only tests pass under sudo; mutation (removing the refusal in send_within) fails a_client_refuses_a_server_run_by_another_user.
- Full suite after the three picks: two integration tests failed. (1) topology a_root_holding_a_custom_state_root_or_the_configuration_is_refused (F-H25): prepare_state_root in resolve_state_root created the state root before the refusal → new locate_state_root (no create); sync-config and watch check OwnState first, then prepare. (2) supervisor a_name_with_control_characters_is_printed_escaped (f5ff447): mixed stderr into its JSON; this box's umask 002 makes config 0664 → LOCAL-02's warning. Test now parses stdout only. Both in follow-up commit 04839d0 (couldn't amend into 1a8cb73 without rewriting history).
