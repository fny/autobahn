## Tickets, in order

1. `T2-1-scan-delta-header.md`
2. `F-M-SUP-supervisor-robustness.md`: **only M-12 part A**.
   - Setup deadlines: handshake, probe, upload and channel open.
   - Don't hold the pool lock across network waits.
   - Client-side timeouts on control-socket calls, in `src/supervisor/control.rs`'s client functions only; the server side is lane E1's.
   - The tray refresh uses the client timeout.

   Part B, the progress frames, is a wave 2 epoch change: don't do it.
3. `OPS-3-posix-install-script.md`
4. `OPS-4-ssh-options.md`
5. `F-M-OUT-untrusted-text.md`: **only M-8**, the prune allowlist plus `crate::text::shell_quote`, and the relayed-stderr part, which sanitizes and caps relayed agent stderr lines.
6. From `F-L-MISC-small-correctness.md`:
   - L-9: validate the `Response::Scan` arm. Don't remove the variant; that happens in wave 2.
   - L-22: surface the remote's stderr in upload errors.

## Region you own

- `src/transport/mux.rs`
- `src/transport/install.rs`
- `src/transport/mod.rs`, except `create_endpoint`, which lane 0b finished
- `src/endpoint/remote.rs`
- `Signature::validate` in `src/rsync/mod.rs`
- the client functions in `src/supervisor/control.rs`

Lane B2 only changes thread-spawn call lines in `mux.rs` and `transport/mod.rs`, so leave those lines alone.
