## Tickets, in order

1. `T1-1-supply-confinement.md`
2. `T1-5-staging-request-paths.md`
3. `LOCAL-03-staging-temporaries.md`: **only** the `open_receive_file` and `stage_locally` temporaries.
4. `F-H7-streaming-supply.md`
5. `F-M-STAGE-staging-and-apply.md`: **only M-31**, where one file's changed base must not fail the whole stream.

## Region you own

In `src/endpoint/local.rs`:
- the supply side: `supply_open`, `supply_pull`, `buffer_delta`, `try_supply`, `supply_from`, `SupplyState`, `digest_paths`, and a new `snapshot_file`;
- the receive side: `stage_begin`, `open_receive_file`, `push_frames` and the receive state, `stage_locally`, and `base_signature`.

In `src/rsync/mod.rs`: only what F-H7's producer thread needs.

Lane A2 owns the apply and publish side of `local.rs`. Lane B1 owns the watcher and `transition`'s generation handling. Lane 0c's `crate::fsutil::private_file` exists: use it for temporaries.
