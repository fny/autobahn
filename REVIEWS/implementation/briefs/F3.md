## Tickets, in order

`CI-02`, `CI-03`, `CI-04`, `CI-06`, `CI-07`, `CI-08`, `CI-09`, `CI-10` (files `REVIEWS/fixes/CI-*.md`). Also these workflow-side pieces from other tickets:
- **REL-1 step 1:** publish `install.sh` as a release asset.
- **CI-05's workflow half.** In the macOS release job, build first with `apps/macos/build.sh --unsigned`, then import the certificate, then sign with `apps/macos/release.sh --sign-only <app>`. Lane F5 implements both script flags, with exactly those names.
- **HYG-1's release check:** the built app's plist version must equal the tag.

## Region you own

- everything under `.github/`
- `spec/check.sh`
- `spec/tla2tools.version`, a new file
- `deny.toml`, a new file

## Details

- **Pinning actions (CI-06).** You can't run GitHub Actions here. Resolve action SHAs with `git ls-remote https://github.com/<owner>/<repo> refs/tags/<tag>`.
- **Pinning the TLA+ jar (CI-03).** Download the chosen `tla2tools.jar` release and compute its SHA-256 locally.
- **Checking the workflows.** Install `actionlint` (a Go binary release) and lint every workflow. Test `spec/check.sh` with the stub-`java` cases from CI-02, and run `spec/check.sh quick` for real: Java 17 is installed.
- **Isolation check (CI).** F-M-TEST asks for a CI check that a test run leaves `~/.autobahn` unchanged. Add it to the Linux job.
