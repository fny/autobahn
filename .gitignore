/target
# Local agent builds; releases build and publish their own
/dist/
bench/verify/hosts-*.json
apps/macos/Autobahn.app/

# TLC scratch (spec/check.sh)
spec/states/

# A/B, burst and matrix runs made here; the published ones are committed deliberately
bench/results-bench-*/
