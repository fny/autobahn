## Tickets, in order

1. `REL-1-release-integrity.md`: **step 1 only**. That covers `scripts/install.sh`, the updater's error message, publishing `install.sh` as a release asset (the workflow side belongs to lane F3; just note it), and pointing the README one-liner at the release asset.
2. `LOCAL-06-update-workspace.md`
3. `F-M-UPD-updater-and-service.md`
4. `LOCAL-08-refuse-root.md`. The CLI entry-point checks in `src/main.rs`'s dispatch are allowed as a small, separate function. The agent-side check goes in `serve_agent` or `create_endpoint`'s caller, as a few lines.

## Region you own

- `src/update.rs`
- `src/service.rs`
- `scripts/install.sh`
- the README install line
- the small entry-point checks named above

Use `crate::fsutil`, which lane 0c added.
