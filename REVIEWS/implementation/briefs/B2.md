## Tickets, in order

1. `F-H4-directory-swap-rescan.md`
2. `LOCAL-09-scanner-file-opens.md`
3. `F-L-STATE-durability-nits.md`: **only L-26**, recent-write protection on adopted subtrees.
4. `F-H25-root-contains-state.md`: **only the scanner backstop**. The scanner always excludes the state root when it falls inside a root, and the agent excludes its own `~/.autobahn`. Lane D2 owns the planning-time refusal.
5. `F-H5-deep-tree-stack.md`, all of it: add a small `src/threads.rs` with `DEEP_STACK` and `spawn_deep` (plus a scoped variant), the scanner depth cap, an iterative `Drop` for `Node`, and the big stacks at every spawn site:
   - scan helpers;
   - session workers in `src/supervisor/mod.rs`;
   - the router and agent channel threads in `src/transport/mux.rs` and `src/transport/mod.rs`;
   - the one-shot `sync` thread in `src/main.rs`.

   The spawn-site edits outside your region must change only the spawn call itself, since other lanes are editing nearby code.

## Region you own

- `src/scan/mod.rs`
- `src/scan/probes.rs`
- a new `src/threads.rs`
- `Drop` for `Node` in `src/tree/mod.rs`
- the spawn call lines named above
