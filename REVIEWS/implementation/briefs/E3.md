## Tickets, in order

1. `F-H27-alert-hook-applescript.md`:
   - pass the summary to `osascript` as an argument in `ON_ALERT_EXAMPLE` (`src/config.rs`; editing that string is allowed);
   - sanitize the summary and detail at composition, with `crate::text::display_safe` and a length cap;
   - rewrite an exact previously-shipped copy of the example hook at startup.

   Test the example's quoting with an `osascript` stub.
2. `F-M-SUP-supervisor-robustness.md`: **only M-35**. Write the hook's stdin and drain its stderr on separate threads within the deadline, and kill the process group on timeout.

## Region you own

- `src/alerts.rs`
- the `ON_ALERT_EXAMPLE` string in `src/config.rs`
- a small startup call for the hook rewrite, which can live in `alerts.rs` and be called from where alerts are set up
