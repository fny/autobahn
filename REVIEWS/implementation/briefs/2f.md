## Tickets: CI and tooling follow-ups from wave 1

1. **`apps/macos/test.sh`,** lane F5's standalone shell test, runs as a step in the Linux CI job.
2. **`--locked`** on the cargo build in `apps/macos/build.sh` (CI-07's leftover).
3. **`bench/harness`:** `cargo fmt` the pre-existing unformatted files (`agents.rs`, `partitions.rs`, `sampler.rs`, `walk.rs`), as a formatting-only commit.
4. **shellcheck:** make `bench/verify/fanout.sh` and `bench/alpha-bench.sh` pass `shellcheck`.
5. **`examples/cycle_cost`** panics intermittently on small trees ("one edit should yield one transition"), and did so before wave 1 too. Find out why and fix it. It may be a test-harness timing assumption, or a real bug. If it is a real bug in the engine, stop and write it up in your notes instead of fixing engine code.
6. **Isolation check:** confirm the CI check that a test run leaves `~/.autobahn` unchanged exists (F-M-TEST M-53, lane F3). Add it if it doesn't.

## Region you own

- `.github/`
- `apps/macos/`
- `bench/`
- `examples/`
