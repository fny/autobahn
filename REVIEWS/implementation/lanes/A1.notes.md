# Lane A1 notes

- Start: wave-0 at a9caba5.
- T1-1 done. Supply gate lives in new `open_scanned` (validate_path + snapshot_file + digest + O_NOFOLLOW|O_NONBLOCK + inode/size + take). Tests in new `mod confinement_tests` at end of local.rs (separate module to avoid merge clashes with other lanes appending to `mod tests`). Test 7 (growth): the size check refuses a file grown before open; growth after open is covered by take(), tested again under F-H7. INVARIANTS wording left for wave-2 docs lane.
- T1-5 done. stage_begin refuses invalid/empty/reserved-prefix paths up front (the reserved-prefix check is local, duplicating T1-4's validate_name change in lane A2 — harmless, can be dropped once A2 lands). resolve_confined (T1-3, lane A2) isn't on this branch, so a local `open_base(root, path)` lstat's each parent and opens O_NOFOLLOW|O_NONBLOCK + fstat; integrator may swap it for resolve_confined. `base_signature` now takes an opened File. Receive-state discard moved to the top of stage_begin so a refused batch still drops the stale stream.
- LOCAL-03 (open_receive_file + stage_locally only) done. New `staging_temporary(purpose)` wraps fsutil::private_file with retry-on-EEXIST (8 tries) and a path-free error. stage_locally no longer calls copy_verifying (A2's publish path still does); it opens via open_scanned and digests via DigestingWriter. Did NOT touch temporary_name (random component is A2's). The planted-symlink test predicts names from TEMPORARY_COUNTER + pid; once A2 randomizes temporary_name, the plant misses and the test still passes (just vacuously) — integrator may want to adjust it then.
- F-H7 done. buffer_delta/try_supply/supply_from are gone; replaced by next_supply_frame + begin_supply + SupplySource {Whole, Delta, Failed} + ScannedFile (Read that caps at scanned size and errors on early EOF). Delta runs on a thread "autobahn-supply" over sync_channel(SUPPLY_TARGET_BYTES / MAX_DATA_OP = 128). Fallback to alternate paths now happens only at open time (before Begin). Memory test is a new integration binary tests/supply_memory.rs (counting global allocator; uses sparse files so it costs no disk). Measured: 256 MiB peak ~8.4 MB, bound 32 MiB.
- F-M-STAGE M-31 done: patch failure in push_frames → discard + Receiving::Sink; need stays in ReceiveState.needs. In the test the transition reports two.bin as a problem (changed since scan) rather than missing content, since its base was the thing that changed; either keeps it unpublished.
