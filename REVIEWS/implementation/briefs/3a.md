## Tickets: mop-up of the remaining small items

1. **`HYG-4-code-nits.md`, what's left.**
   - Remove `let _ = shown;`, `let _ = inner;`, `let _ = root;` and `let _ = intent_recorded;`, plus the values they discard if those are unused.
   - Remove the immediately-invoked closure in `attempt_once`.
   - Use `unwrap_or_else(PoisonError::into_inner)` where `.expect` is used on poisonable mutexes in `src/transport/mux.rs`.
   - Add a test asserting `protocol::version()` matches `[0-9A-Za-z._+-]+`.
   - Replace any remaining `unsafe { libc::isatty }` with `std::io::IsTerminal`.

   Grep for each first, since some may already be gone.
2. **One-shot endpoints for commands that never wait.** `open_endpoints`, used by `doctor`, `diff` and `resolve`, still opens endpoints in watching mode. Pass `one_shot = true` for them. Measure `autobahn diff` on a large tree before and after, and put the numbers in the commit.
3. **M-46's deterministic tests.** In the observer tests (`src/endpoint/observer.rs`), add deterministic tests, with test hooks rather than timing, for:
   - a late watcher event arriving after a cycle already scanned;
   - the polling fallback picking up an unannounced external write.
4. **`bench/verify/fanout.sh`** hard-codes `/home/ubuntu/Workspace/autobahn`. Derive the repo root from the script's location.
5. **`resolve`'s flush.** After a resolve, flush only the sessions that were touched, not the whole group, when the control protocol allows naming them. Skip this if it needs a protocol change.

## Region you own

The code named in each item. Keep each change minimal.
