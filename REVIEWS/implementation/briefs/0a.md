## Tickets

`CI-01-clippy-green.md`, plus reformatting.

## Region you own

- `enum Receiving` in `src/endpoint/local.rs`, and the lines that construct or match it.
- The two `type_complexity` sites in `src/endpoint/remote.rs`.
- Formatting only in `src/transport/mux.rs`.

## Details

1. **Clippy.** Box the large variant's payload of `Receiving` so the enum is small. Don't silence the lint. Name the two complex types with `type` aliases, or small structs if the fields deserve names.
2. **Formatting.** Run `cargo fmt`. The installed stable rustfmt, which is what CI uses, reformats `src/transport/mux.rs`. Commit that reformatting as its own commit, `style: rustfmt on current stable`, with no other changes in it.

Done means `cargo fmt --check` and `cargo clippy --release --all-targets -- -D warnings` are both clean, and the full suite is no worse than the baseline.
