## Tickets, in order

1. `F-C1-emptied-root-untracked.md`, including the proptest generator that places `Untracked` at every depth, and the "no silent loss" property.
2. `F-H8-excluded-content-deletion.md`: **only the reconcile part**. An entry that is `Untracked` on one side but present in the ancestor blocks a sibling or parent deletion. Also fix the `blocking` comment. Lane A2 owns the endpoint part.
3. `F-M-STATE-journal-and-reconcile.md`: **only M-33**, `old: beta_sync` in the one-way modes.
4. `F-L-MISC-small-correctness.md`: **only L-31**. Write the reconcile test for "alpha deletes a file beta edited" in all five modes, then fix whichever of the code and `docs/modes.md` is wrong. The doc edit is allowed.

## Region you own

- `src/tree/reconcile.rs`
- `src/tree/mod.rs`, except `Drop`, which is lane B2's
- `one_side_emptied_root` and the emptied-root halt in `src/session/mod.rs`
- the reconcile proptests
- `docs/modes.md`, for L-31 only
